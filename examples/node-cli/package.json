{
  "name": "@examples/node-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "pnpm run typecheck",
    "dev": "node --env-file-if-exists=../../.env --import tsx src/sign-hash.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ltorresu82/firmagob-client": "^0.1.3"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "tsx": "^4.22.4"
  }
}
