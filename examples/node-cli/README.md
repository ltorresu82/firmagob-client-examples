# Node CLI

CLI minima para firmar un hash SHA-256 base64 de un PDF preparado.

```bash
npm run node-cli -- HASH_SHA256_BASE64
```

Requiere las variables `FIRMAGOB_*` documentadas en la raiz del repositorio.
