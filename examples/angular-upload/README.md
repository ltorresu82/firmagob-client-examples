# Angular Upload

App Angular minima para enviar un hash PDF a la API Hono local.

```bash
pnpm dev:angular
```

Luego abrir `http://127.0.0.1:4200`.

La app espera que `pnpm dev:hono` este escuchando en `http://localhost:8787`.
