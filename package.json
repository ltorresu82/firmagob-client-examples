{
  "name": "firmagob-client-examples",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "check": "npm run typecheck --workspaces --if-present && npm run build --workspaces --if-present",
    "node-cli": "npm --workspace @examples/node-cli run dev --",
    "hono-api": "npm --workspace @examples/hono-api run dev",
    "astro-upload": "npm --workspace @examples/astro-upload run dev"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
